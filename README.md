# IT5FinalProject_POS_System

Requires: PyQt5 in the environment if not still installed type the bellow command in the python terminal.

Install: pip install PyQt5

To run: python grocery_pos.py

Revisions:
I have now added the concept of OOP. I now made my database connection on a separate file. See "database_manager.py"
